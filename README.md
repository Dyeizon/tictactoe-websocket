# 'Tic Tac Toe' with WebSocket

## server (NodeJS)

**Package installation**: `npm install` <br/>
**Run**: `node ./index.js`

## client (ReactJS)

**Package installation**: `npm install` <br/>
**Run**: `npm start` 
